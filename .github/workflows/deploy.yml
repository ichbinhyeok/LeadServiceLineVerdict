name: Manual OCI deploy for Lead Line Record

on:
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Make Gradle wrapper executable
        run: chmod +x ./gradlew

      - name: Run tests
        run: ./gradlew test

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push image
        id: build_image
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/arm64
          push: true
          tags: |
            shinhyeok22/leadline:latest
            shinhyeok22/leadline:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Prepare OCI deploy directory
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.OCI_HOST }}
          username: ${{ secrets.OCI_USERNAME }}
          key: ${{ secrets.OCI_KEY }}
          script: |
            mkdir -p ~/deploy/leadline/logs
            cat <<EOF > ~/deploy/leadline/.env
            IMAGE_TAG=${{ github.sha }}
            EOF

      - name: Copy compose file to OCI
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.OCI_HOST }}
          username: ${{ secrets.OCI_USERNAME }}
          key: ${{ secrets.OCI_KEY }}
          source: "docker-compose.yml"
          target: "~/deploy/leadline"
          overwrite: true

      - name: Deploy to OCI
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.OCI_HOST }}
          username: ${{ secrets.OCI_USERNAME }}
          key: ${{ secrets.OCI_KEY }}
          script: |
            cd ~/deploy/leadline
            docker compose pull
            docker compose up -d
            docker image prune -f

      - name: Verify OCI container and image
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.OCI_HOST }}
          username: ${{ secrets.OCI_USERNAME }}
          key: ${{ secrets.OCI_KEY }}
          script: |
            set -e
            cd ~/deploy/leadline
            docker compose ps
            docker image inspect shinhyeok22/leadline:${{ github.sha }} --format '{{json .RepoDigests}}'
            test "$(docker inspect leadline --format '{{.Config.Image}}')" = "shinhyeok22/leadline:${{ github.sha }}"

      - name: Verify OCI local homepage on port 8094
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.OCI_HOST }}
          username: ${{ secrets.OCI_USERNAME }}
          key: ${{ secrets.OCI_KEY }}
          script: |
            set -e
            for attempt in 1 2 3 4 5 6; do
              if HOME_HTML=$(curl -sSf http://127.0.0.1:8094/); then
                break
              fi
              echo "homepage attempt $attempt failed; retrying..."
              sleep 5
            done
            test -n "$HOME_HTML"
            echo "$HOME_HTML" | grep -o "<title>[^<]*</title>" | head -n 1
            echo "$HOME_HTML" | grep -q "Lead Line Record"

      - name: Verify OCI local lookup route
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.OCI_HOST }}
          username: ${{ secrets.OCI_USERNAME }}
          key: ${{ secrets.OCI_KEY }}
          script: |
            set -e
            for attempt in 1 2 3 4 5 6; do
              LOOKUP_STATUS=$(curl -s -o /tmp/leadline-lookup.html -w "%{http_code}" http://127.0.0.1:8094/lookup || true)
              echo "lookup_status=$LOOKUP_STATUS"
              if [ "$LOOKUP_STATUS" = "200" ]; then
                break
              fi
              echo "lookup attempt $attempt failed; retrying..."
              sleep 5
            done
            echo "lookup_status=$LOOKUP_STATUS"
            test "$LOOKUP_STATUS" = "200"

      - name: Verify OCI local utility route
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.OCI_HOST }}
          username: ${{ secrets.OCI_USERNAME }}
          key: ${{ secrets.OCI_KEY }}
          script: |
            set -e
            for attempt in 1 2 3 4 5 6; do
              UTILITY_STATUS=$(curl -s -o /tmp/leadline-utility.html -w "%{http_code}" http://127.0.0.1:8094/lead-service-line/dc/washington/dc-water || true)
              echo "utility_status=$UTILITY_STATUS"
              if [ "$UTILITY_STATUS" = "200" ]; then
                break
              fi
              echo "utility attempt $attempt failed; retrying..."
              sleep 5
            done
            echo "utility_status=$UTILITY_STATUS"
            test "$UTILITY_STATUS" = "200"

      - name: Verify public domain
        run: |
          set -e
          for attempt in 1 2 3 4 5 6; do
            if HOME_HTML=$(curl -sSf https://leadlinerecord.com/); then
              break
            fi
            echo "public homepage attempt $attempt failed; retrying..."
            sleep 5
          done
          test -n "$HOME_HTML"
          echo "$HOME_HTML" | grep -o "<title>[^<]*</title>" | head -n 1
          echo "$HOME_HTML" | grep -q "Lead Line Record"
          for attempt in 1 2 3 4 5 6; do
            LOOKUP_STATUS=$(curl -s -o /tmp/public-lookup.html -w "%{http_code}" https://leadlinerecord.com/lookup || true)
            echo "public_lookup_status=$LOOKUP_STATUS"
            if [ "$LOOKUP_STATUS" = "200" ]; then
              break
            fi
            echo "public lookup attempt $attempt failed; retrying..."
            sleep 5
          done
          echo "public_lookup_status=$LOOKUP_STATUS"
          test "$LOOKUP_STATUS" = "200"
          for attempt in 1 2 3 4 5 6; do
            UTILITY_STATUS=$(curl -s -o /tmp/public-utility.html -w "%{http_code}" https://leadlinerecord.com/lead-service-line/dc/washington/dc-water || true)
            echo "public_utility_status=$UTILITY_STATUS"
            if [ "$UTILITY_STATUS" = "200" ]; then
              break
            fi
            echo "public utility attempt $attempt failed; retrying..."
            sleep 5
          done
          echo "public_utility_status=$UTILITY_STATUS"
          test "$UTILITY_STATUS" = "200"
